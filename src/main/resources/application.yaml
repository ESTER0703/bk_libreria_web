spring:
  application:
    name: api

  datasource:
    url: jdbc:postgresql://localhost:5432/basebiblioteca
    username: administrador
    password: cambiar_por_password_local

  jpa:
    hibernate:
      ddl-auto: update

    show-sql: true

    properties:
      hibernate:
        format_sql: true

  web:
    resources:
      static-locations: classpath:/static/,file:uploads/

jwt:
  secret: "LibreriaWebSecretKey"
  expirationMs: 86400000

